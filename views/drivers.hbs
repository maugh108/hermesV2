{{#if drivers.length}}
<div class="container-fluid mt-4">
    <div class="row">
        <!--<div class="col-4 px-5 justify-content-center" style="height: 90vh;">
            <div class="bg-primary  h-100 text-center">
                <h1 class="text-white ">Ordenes</h1>
                {{#each orders as |order|}}
                <div class="card mx-5" >
                    <div class="card-body">
                        <h5 class="card-title">{{driver.name}}</h5>
                        <h6 class="card-subtitle mb-2 text-muted">{{driver.phone}}</h6>
                        <p class="card-text">{{driver.license}}</p>
                        <p class="card-text">{{driver.city}}</p>
                    </div>
                </div>
                {{/each}}
            </div>
            <div>
            </div>
        </div>
        -->
        <div class="col-12 mx-5 justify-content-center" style="height:90vh">
            <button type="button" class="btn btn-success"  onclick="CreateDriver()">Crear Nuevo Conductor</button>
            <table class="table table-dark">
                <thead>
                    <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Driver name</th>
                    <th scope="col">Driver License</th>
                    <th scope="col">Driver City</th>
                    <th scope="col">Driver Phone</th>
                    <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    {{#each drivers as |driver|}}
                    <tr>
                        <td scope="row">{{id}}</th>
                        <td>{{name}}</td>
                        <td>{{license}}</td>
                        <td>{{city}}</td>
                        <td>{{phone}}</td>
                        <td>
                            <button type="button" class="btn btn-primary" id="{{id}}" onclick="UpdateDriver({{id}})">Editar</button>
                            <button type="button" class="btn btn-danger" onclick="DeleteDriver({{id}})">Borrar</button>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
                </table>
        </div>
    </div>
</div>
{{/if}}

<script src="/javascript/driver.js"></script>